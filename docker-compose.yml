version: '3.8'

services:
  app:
    build: .
    container_name: todo-api
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - LOG_LEVEL=info
    restart: unless-stopped

